{
  "hooks": {
    "PostPush": [
      {
        "type": "command",
        "command": "echo '📝 Documentation check: Review whether the changes you just pushed require documentation updates. If code behaviour, APIs, configuration, or architecture changed, update the relevant docs (README, docs/, CLAUDE.md) before considering this work complete.'"
      }
    ]
  }
}
